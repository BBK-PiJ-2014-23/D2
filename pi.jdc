int n = Integer.parseInt(System.console().readLine());
double pi = 0;

for (int i = 0; i < n; i++) {
	if (i % 2 == 0 || i == 0) {
		pi = pi + 4.0 / (2 * i + 1);
	} else {
		pi = pi - 4.0 / (2 * i + 1);
	}
}
println(pi);